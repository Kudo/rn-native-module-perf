package com.testmodule;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class TestmoduleSpec extends NativeTestmoduleSpec {
  TestmoduleSpec(ReactApplicationContext context) {
    super(context);
  }
}
